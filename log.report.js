/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('log.report');
 * mod.thing == 'a thing'; // true
 */

var reportInternal = 20000; //Interval for updating and sending out report
var sendReportNotification = true; //Should send as notification instead of logging to console

module.exports = {

};